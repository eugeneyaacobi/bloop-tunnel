services:
  client:
    build:
      context: ../..
      dockerfile: deploy/docker/client.Dockerfile
    command: ["--config", "/config/client.yaml"]
    volumes:
      - ../examples/client.example.yaml:/config/client.yaml:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
